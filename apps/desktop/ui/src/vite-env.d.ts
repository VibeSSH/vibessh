/// <reference types="vite/client" />

// Brings in `import.meta.glob` and friends. The guide loads its markdown and
// its screenshots through it (`src/guide/guideDocs.ts`), which is what made
// this file necessary - before that nothing in `src/` used a Vite-specific
// part of `import.meta`.
