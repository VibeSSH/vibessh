{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main VibeSSH window",
  "windows": ["main"],
  "permissions": ["core:default", "log:default", "dialog:default"]
}
