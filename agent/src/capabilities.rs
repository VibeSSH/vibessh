//! Reserved for capability reporting (Etap I): which optional subsystems
//! this host actually has (docker, systemd, minecraft, file_access,
//! terminal), so the desktop UI can hide/gray out features the agent can't
//! serve instead of assuming every Linux host has everything.
